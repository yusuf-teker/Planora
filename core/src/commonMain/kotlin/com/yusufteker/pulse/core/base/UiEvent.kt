package com.yusufteker.pulse.core.base

/**
 * Marker interface for all UI events in the MVI pattern.
 *
 * Events represent user interactions or system triggers that
 * are dispatched from the UI layer to the ViewModel.
 *
 * Each screen defines its own sealed interface implementing this.
 */
interface UiEvent
